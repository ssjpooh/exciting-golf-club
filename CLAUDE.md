# CLAUDE.md — 세션 인수인계 & 작업 로그

> 이 파일은 **어떤 AI(클로드/기타)로 접속하든** 이전 세션에 무슨 일이 있었는지 이어받도록 하는 인수인계 문서다.
> **새 세션을 시작하면 먼저 이 파일과 `AGENTS.md`를 읽을 것.**

## 📌 먼저 읽을 것
- 앱 구조·도메인 로직·데이터 모델·관례 → **`AGENTS.md`** (상세). 이 파일은 그 위에 "무슨 작업을 언제 왜 했는지" 시간순 로그만 관리한다.

## ✅ 반드시 지킬 규칙
1. **작업을 마치면 아래 "작업 로그"에 새 항목을 맨 위에 추가한다** (날짜 / 무엇 / 왜 / 바꾼 파일).
2. 구조·도메인·관례가 바뀌면 `AGENTS.md`의 관련 섹션도 갱신한다.
3. 완료 후 항상 `main`에 커밋 & 푸시한다 (묻지 않고 자동, 한국어 커밋 메시지).
4. UI/UX는 상식적·편의 우선으로 판단한다 (옵션 적고 자주 토글 → 세그먼트 버튼, `fontSizePreset` 4단계 모두 고려, teal 테마).

---

## 🗓️ 작업 로그 (최신이 위)

### 2026-07-18 — 구글 로그인 삭제
- **무엇**: 로그인 페이지에서 구글 로그인 버튼 및 관련 분기(카카오톡 인앱 브라우저 외부 열기 우회 `isKakao` 포함) 제거. 이제 노출되는 로그인은 **카카오톡뿐** (네이버/애플은 기존부터 주석, 익명은 미노출 죽은 코드).
- **왜**: 사용자 요청.
- **파일**: `src/routes/index.tsx`. (`providers.ts`의 `signInWithGoogle` 함수는 재활성화 대비 유지)
- **주의**: Firestore 규칙의 super_admin 자동 승급은 토큰에 이메일이 실리는 구글/애플만 가능했으므로, 구글 로그인이 사라진 지금 **신규 관리자 승급은 Firebase 콘솔에서 수동**으로 해야 함(기존 super_admin 계정은 영향 없음).

### 2026-07-18 — Firestore 보안 규칙 작성 (권한 상승 취약점 대응)
- **무엇**: 기존 콘솔 규칙이 `request.auth != null`이면 전체 read/write 허용 → 아무 로그인 유저나 자기 role을 super_admin으로 바꾸거나 남의 기록 삭제 가능했음. 컬렉션별 규칙(`firestore.rules` 신규, 버전 관리용 사본)으로 교체: users(본인+관리자), scores(본인 기록만, 관리자 열람), golf_courses(공유 DB), 그 외 차단.
- **왜**: 앱 전체 점검에서 발견. 클라이언트 role 체크는 우회 가능하므로 서버측 방어 필요.
- **파일**: `firestore.rules`(신규), `src/lib/db.ts`(`createOrUpdateUser` — 카카오/네이버 커스텀 토큰은 이메일 클레임이 없어 super_admin 자동 생성/승급이 규칙에 거부될 수 있으므로 member로 폴백 재시도), `AGENTS.md`.
- **주의**: **규칙은 Firebase 콘솔에 직접 붙여넣어 "게시"해야 적용됨**(저장소 파일은 사본). 콘솔 규칙과 파일을 항상 동일하게 유지. 카카오/네이버 계정의 관리자 승급은 구글 계정 관리자가 수동으로 해야 함.

### 2026-07-18 — 카카오 친구 초대(공유) 기능 + 설정(톱니바퀴) 메뉴
- **무엇**: 스코어 페이지(`/scores`) 헤더의 **로그아웃 버튼 → 톱니바퀴(설정) 드롭다운**으로 변경. 메뉴 안에 **친구 초대** + **로그아웃** 2개. 친구 초대는 카카오 JS SDK `Kakao.Share.sendDefault`(피드형)로 카카오톡 공유창을 띄워 앱 링크를 보냄(링크 공유형, 별도 심사 불필요).
- **왜**: 사용자 요청. 헤더 우측 정리 + 카카오로 지인 초대.
- **파일**: `src/lib/kakao.ts`(신규 — SDK 동적 로더 + `inviteFriendsViaKakao`), `src/routes/scores.tsx`(헤더 드롭다운, `isSettingsMenuOpen` 상태, `handleInviteFriends`, 아이콘 `Settings`/`UserPlus` 임포트), `.env`(`VITE_KAKAO_JAVASCRIPT_KEY` 추가), `AGENTS.md`.
- **주의**: 공유하기는 **JavaScript 키**(`VITE_KAKAO_JAVASCRIPT_KEY`)를 쓴다(로그인용 REST API 키와 별개, 같은 앱). 동작하려면 카카오 개발자 콘솔 > 앱 설정 > **플랫폼 > Web 에 서비스 도메인 등록** 필요. 미등록 시 공유창이 안 뜬다.

### 2026-07-14 — 세션 인수인계 체계 구축
- **무엇**: `CLAUDE.md`(이 파일) 신규 생성. `AGENTS.md`에 §0 "작업 기록 규칙" 추가.
- **왜**: 어떤 AI로 접속해도 이전 작업 맥락을 이어받도록. 작업 후 항상 로그를 남기는 규칙 확립.
- **파일**: `CLAUDE.md`(신규), `AGENTS.md`.

### 2026-07-14 — 볼링 앱 잔재 제거
- **무엇**: 과거 볼링 앱에서 넘어온 문구를 골프로 정리.
- **왜**: 이 앱은 원래 볼링 스코어 앱 → 골프로 전환됨. 사용자에게 보이는 볼링 문구 잔재 제거 요청.
- **파일**: `src/lib/version.ts`(릴리스 노트), `src/components/ReleaseNoteDialog.tsx`(제목/설명), `src/lib/db.ts`(`createOrUpdateUser`의 `"볼링러"` 닉네임 마이그레이션 제거), `AGENTS.md`.
- **주의**: `db.ts`에서 `"볼링러"` 체크를 제거해, 아직 닉네임이 "볼링러"인 초오래된 계정은 자동 교체되지 않음(edge case).

### 2026-07-14 — AGENTS.md 최초 작성
- **무엇**: 앱 구조/도메인 로직/데이터 모델/인증/관례/알려진 이슈/개발 히스토리 종합 문서화.
- **왜**: 다른 AI가 수정 시 히스토리를 파악하도록.
- **파일**: `AGENTS.md`(신규).

### 2026-07-14 — 라운딩 종류/타입 기능 (핵심)
아래는 한 흐름으로 이어진 작업. 최종 상태 = **① 기록 저장은 필드/스크린, ② 조회 필터는 전체/필드/스크린, ③ 필터 버튼은 조회 옵션 바에, ④ 통계 그래프 제목만 선택값 반영.**
1. **라운딩 종류 추가**: `Score.roundType`(`field`/`screen`, 코드 저장) 도입. 기록 입력/수정 다이얼로그의 "코스/코스조합 이름" 하단에 필드/스크린 선택(토글) 추가. 매핑 상수 `ROUND_TYPES`·타입 `RoundTypeCode`는 `src/lib/db.ts`. 다이얼로그에는 **필드/스크린 2개만**(전체 없음). 레거시 기록은 `?? 'field'`.
   - 파일: `src/lib/db.ts`, `src/routes/scores.tsx`.
2. **홈 화면/통계 필터**: "홈으로" 이동 시 `courseInfo`를 `null`로 비워 첫 로그인 화면 복귀. 통계에 라운딩 타입 필터 도입. `ScoreStatisticsGraph`.
3. **리스트도 필터 적용**: 라운딩 타입 필터를 `displayGames`에 통합(통계+리스트 공통). 기록 카드 "그로스(기본):" 배지에 `[필드]`(초록)/`[스크린]`(남보라) 표시.
4. **UX 개선**: 라운딩 타입 필터를 드롭다운 → **세그먼트 버튼**화, 입력 창 글자 크기와 한 줄 배치(공백 축소).
5. **최종 배치 정리**: 필터 버튼은 **조회 옵션 바**에 유지, **그래프 제목만** "전체/필드/스크린 스코어 통계"로 동적. `ScoreStatisticsGraph`는 `onRoundTypeChange`가 있을 때만 헤더 버튼 렌더(중복 제거).
   - 파일: `src/routes/scores.tsx`, `src/components/ScoreStatisticsGraph.tsx`.

---

## 🧭 다음 세션이 참고할 열린 항목(TODO 후보)
- `src/routes/index.tsx`가 존재하지 않는 `getUserActiveApprovalRequest`를 import → tsc 에러(§11/AGENTS.md). 승인요청 기능 미완성 흔적. 필요 시 정리.
- 슈퍼 관리자 이메일 목록이 `db.ts`와 `scores.tsx`에서 불일치.
- (요청 시) 라운딩 타입 필터를 리스트에도/통계에만 적용할지 등 UX 미세조정 여지.
